const MODEL_NAMES = require('./model-names.enum')
const ROLE = require('./role.enum')
const KEY = require('./key.enum')
const OrderStatus = require('./orderStatus.enum')
const DeliveryMethod = require('./deliveryMethod.enum')

module.exports = {
    MODEL_NAMES,
    ROLE,
    KEY,
    OrderStatus,
    DeliveryMethod
}