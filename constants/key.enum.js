const KEY = Object.freeze({
    ADMIN_PASSWORD: 'admin123',
    OBJECT_ID_PATTERN: /^[0-9a-fA-F]{24}$/,
    MESSAGES: {
        OBJECT_ID_WRONG_FORMAT: 'Sai định dạng #label'
    }
})

module.exports = KEY